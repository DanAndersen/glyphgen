/*global app*/
app.factory('Glyph', [function () {
    'use strict';

    function Glyph(strokes) {
        this.strokes = strokes;
    }

    Glyph.prototype = {
        
    };

    return Glyph;
}]);
