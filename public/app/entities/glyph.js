/*global app*/
app.factory('Glyph', [function () {
    'use strict';

    function Glyph(segments) {
        this.segments = segments;
    }

    Glyph.prototype = {
        
    };

    return Glyph;
}]);
